import 'package:flutter/material.dart';

/// Authoritative Navy — CA App brand colors
class AppColors {
  AppColors._();

  // Primary
  static const primary = Color(0xFF1B3A5C);
  static const primaryVariant = Color(0xFF2A5B8C);

  // Secondary
  static const secondary = Color(0xFF0D7C7C);

  // Accent
  static const accent = Color(0xFFE8890C);

  // Semantic
  static const success = Color(0xFF1A7A3A);
  static const warning = Color(0xFFD4890E);
  static const error = Color(0xFFC62828);

  // Neutrals
  static const neutral900 = Color(0xFF1A202C);
  static const neutral600 = Color(0xFF4A5568);
  static const neutral400 = Color(0xFF718096);
  static const neutral200 = Color(0xFFE2E8F0);
  static const neutral50 = Color(0xFFF7FAFC);
  static const surface = Color(0xFFFFFFFF);

  // Dark mode surfaces
  static const darkSurface = Color(0xFF1A202C);
  static const darkSurfaceVariant = Color(0xFF2D3748);
}
