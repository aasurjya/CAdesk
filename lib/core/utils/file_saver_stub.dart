/// Stub for conditional import — never called at runtime.
Future<void> saveFile(String content, String fileName) async {
  throw UnsupportedError('Cannot save file without a platform implementation');
}
